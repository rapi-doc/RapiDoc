{
    "openapi": "3.0.0",
    "info": {
        "title": "Hidden readOnly",
        "version": "1.0.0",
        "description": "Hidden readOnly properties for write actions."
    },
    "components": {
        "schemas": {
            "dummy": {
                "type": "object",
                "properties": {
                    "foo": { "type": "string" },
                    "bar": { "type": "string", "readOnly": true }
                }
            }
        },
        "responses": {
            "response": {
                "description": "Dummy response",
                "content": {
                    "application/json": {
                        "schema": { "$ref": "#/components/schemas/dummy" }
                    }
                }
            }
        }
    },
    "paths": {
        "/foo": {
            "post": {
                "summary": "1. Post",
                "description": "Checkout the model tab to see (or not see) the readOnly property `bar`",
                "requestBody": {
                    "content": {
                        "application/json": {
                            "schema": { "$ref": "#/components/schemas/dummy" }
                        }
                    }
                },
                "responses": { "200": { "$ref": "#/components/responses/response" } }
            },
            "put": {
                "summary": "2. Put",
                "description": "Checkout the model tab to see (or not see) the readOnly property `bar`",
                "requestBody": {
                    "content": {
                        "application/json": {
                            "schema": { "$ref": "#/components/schemas/dummy" }
                        }
                    }
                },
                "responses": { "200": { "$ref": "#/components/responses/response" } }
            },
            "patch": {
                "summary": "3. Patch",
                "description": "Checkout the model tab to see (or not see) the readOnly property `bar`",
                "requestBody": {
                    "content": {
                        "application/json": {
                            "schema": { "$ref": "#/components/schemas/dummy" }
                        }
                    }
                },
                "responses": { "200": { "$ref": "#/components/responses/response" } }
            }
        }
    }
}
